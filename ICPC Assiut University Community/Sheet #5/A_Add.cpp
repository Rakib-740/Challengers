#include <bits/stdc++.h>

using namespace std;
#define fast ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
#define endll '\n'

int sum(int x, int y)
{
    return x + y;
}

int main() {
    fast;
    int x, y; cin >> x >> y;

    cout << sum(x,y) << endll;
    
    return 0;
}