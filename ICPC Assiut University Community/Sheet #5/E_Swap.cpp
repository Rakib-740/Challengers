#include <bits/stdc++.h>

using namespace std;
#define fast ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
#define endll '\n'

void swap_two(int a, int b)
{
    cout << b << " " << a;
}

int main() {
    fast;
    int a, b; cin >> a >> b;
    swap_two(a,b);
    return 0;
}