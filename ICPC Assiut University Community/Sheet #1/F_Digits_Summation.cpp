#include <bits/stdc++.h>

using namespace std;
#define fast ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
#define ll long long

int main() {
    fast;
    ll a, b;
    cin >> a >> b;
    int c, d;
    c = a%10;
    d = b%10;
    cout << c + d << endl;

    return 0;
}