#include <bits/stdc++.h>

using namespace std;
#define fast ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);

int main() {
    fast;
    int a;
    cin >> a;

    int temp = a % 10;;

    int first = a / 10;

    if(temp % first == 0)
    {
        cout << "YES" << endl;
    }
    else if(first % temp == 0)
    {
        cout << "YES" << endl;
    }
    else
    {
        cout << "NO" << endl;
    }
    return 0;
}
