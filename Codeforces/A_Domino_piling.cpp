#include <bits/stdc++.h>

using namespace std;
#define endll '\n'

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    
    int m, n;
    cin >> m >> n;

    // int domino = (m * n) / 2;

    cout << (m * n) / 2 << endll;
    return 0;
}